version: "3"

services:
  app:
    build: .
    command: npm run dev
    ports:
      - "3000:3000"
    network_mode: host
    volumes:
      - .:/app
    depends_on:
      - node-db
  mysql-db:
    container_name: "node-db"
    restart: always
    image: mysql:8.0
    ports:
      - "3306:3306"
    network_mode: host
    environment:
      - MYSQL_USER=root
      - MYSQL_ROOT_PASSWORD=123
      - MYSQL_DATABASE=flights
    volumes:
      - pgdata:/app/data

volumes:
  pgdata:
